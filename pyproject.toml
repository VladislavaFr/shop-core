[project]
name = "shop-core"
version = "0.2.0"
description = "Core of e-commerce project with Product and Category classes, including private attributes, getters, setters and classmethods."
authors = [
    {name = "VladislavaFn", email = "vladislava.fridmann1@aol.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "^8.4.2"
pytest-cov = "^7.0.0"
flake8 = "^7.3.0"

